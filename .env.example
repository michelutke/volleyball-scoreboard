DATABASE_URL=postgres://scoring:scoring@localhost:5432/scoring

# Keycloak OIDC
KEYCLOAK_CLIENT_ID=scoring-app
KEYCLOAK_CLIENT_SECRET=
KEYCLOAK_ISSUER=http://localhost:8080/realms/scoring

# Auth.js session encryption (random 32-byte string)
AUTH_SECRET=

# AES-256-GCM key for encrypting Swiss Volley API key in DB (64-char hex = 32 bytes)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# Legacy (no longer used — set Swiss Volley API key via admin settings UI)
# SWISS_VOLLEY_API_KEY=
